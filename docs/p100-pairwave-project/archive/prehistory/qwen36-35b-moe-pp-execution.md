---
name: qwen36-35b-moe-pp-execution
description: "Qwen3.6-35B-A3B prefill optimization EXECUTION progress (stage A+ adopted, B0 deferred, B probe) - measured results per stage on 4xP100"
metadata:
  node_type: memory
  type: project
  originSessionId: 6a047e7a-2e60-4904-ad07-7b1a45f0afaa
---

Execution of the `qwen36-35b-moe-pp-roadmap` plan
(`~/.claude/plans/qwen36-35b-moe-prefill-plan.md`), started 2026-07-21.
Worktree `/home/arian/llama.cpp-q36-moe` (branch `qwen36-35b-moe-pp`, base
`6089050f6`), build `build-p100-moe-pp` (gcc-14 host - CUDA 12.8 rejects
gcc>14). Results in `results/qwen36-35b-moe-pp-20260721/`. Baseline
reproduced: pp8192 539-549, tg ~60-62.

**Stage A+ - ADOPTED (+22.3% pp8192, byte-identical).** Counting-sort transplant in `ggml_cuda_mul_mat_id` cuBLAS fallback (ggml-cuda.cu ~2707), env `GGML_CUDA_MOE_CSORT=1`: replaces the O(experts*tokens*slots)~1e9-iter/ubatch host triple-loop sort with a stable counting sort (token-major/slot order => identical row order => byte-identical). pp8192 539->659 (+22.3%), pp2048 +21.4%, decode flat ~62. Byte-identical on both prompts incl. 4600-tok (drives fallback). Sort-memoization across a layer's up/gate/down (env `GGML_CUDA_MOE_CSORT_REUSE=1`, thread_local cache keyed on ids ggml_tensor object ptr) = marginal +0.7%, byte-identical, kept opt-in. Deploy: `results/.../deploy-35b-moe-pp.sh`. NOTE: this fork's `llama-cli` is conversation-only (spins on EOF); use `llama-completion` for one-shot greedy.

**Stage B0 (merged up+gate) - DEFERRED.** Graph IS merge-ready (ffn_gate_up_exps field + build_moe_ffn single-wide-mul_mat_id + qwen35moe already passes layer.ffn_gate_up_exps) but our GGUF isn't pre-merged and there's no load-time merge-from-separate hook. Post-load merge = +5.7GB/GPU -> OOM at 32K ctx; load-time merge = synthetic non-GGUF tensor + done_getting_tensors n_created accounting surgery in core loader for modest +7.6%. Clean future add once a loader hook exists. Map: `results/.../stageB0-deferred/DEFERRED.md`.

**Stage B (grouped/batched GEMM) - REJECT (measured).** `cublasGemmGroupedBatchedEx` DEAD on P100 (f32 4x slower; f16 NOT_SUPPORTED). `cublasGemmBatchedEx` B1 (chunked, f16-in/f32-out COMPUTE_32F, env `GGML_CUDA_MOE_BGEMM=1`): KLD-clean (ppl 5.8340 vs 5.8319) BUT **~2x SLOWER prefill** (16.4 vs 8.33 s/pass; pp8192 bench hung). Raw batched GEMM is faster (12.5 vs 19.25ms) but the required marshaling - dequant-all-experts->f16 + src1 f16 gather + padded output + compaction kernel + VMM pool churn on variable maxMg - dwarfs the ~7ms/call launch saving. The per-expert cuBLAS loop already fuses dequant + stable pool. Code kept env-gated default-OFF, NEVER enable. B2 (CUTLASS 2.11 SIMT grouped, Sm60) untried - large vendoring gamble. Details `results/.../stageB-batched/RESULTS.md`.

**Layer-split ceiling reached at ~660 (A+).** B0 deferred, B rejected -> the plan's ~1000-1200 layer-split target is NOT reachable via cheap levers on P100; the real path to the user's 3000+ is Stage C (EP).

**Stage C round 2 (same evening) - delayed-allreduce ADOPTED + teardown FIXED.** (a) `get_i_delayed` EP-chain extension (ggml-backend-meta.cpp): gate/up MUL_MAT_ID allreduces ELIMINATED (census 4->2/layer, attn+ffn_moe_out only; guarded SPLIT_AXIS_2 + single-consumer zero-preserving chain GLU/SILU/GELU/RELU/CLAMP/MUL, biased chains bail). Result: tg256 **74.9-75.8 (+20% vs layer 62.7)**, tg128 ~72.5; pp8192 885.7 (-7% vs pre-delay 952, +34% vs layer - merged subgraphs worsen host csort overlap; decode-bound workload => right trade); ppl 5.8341 (in-family, 0.02sigma; drift class = alloc/order, B1-precedent). Adjacent gate/up/GLU also lets the fused mmvq GLU decode kernel fire under EP. (b) **Teardown abort ROOT-CAUSED+FIXED**: in-flight decode graph replay on secondary devices vs buffer frees; fix = `ggml_backend_sched_synchronize` at top of `~llama_context` (+ defensive sync loop in `~ggml_backend_meta_context`); clean EXIT=0 under full graph flags. (c) Debug envs added: GGML_META_DEBUG_ALLREDUCE (census) / GGML_META_DEBUG_SYNC (fault bisector). (d) `deploy-35b-moe-ep.sh` + `bench/ep-soak.sh` staged in results-staging/. **NEXT prefill lever: GPU-side ids sort (mm_ids_helper-style) in the csort fallback** - removes per-mmid D2H+host-sort+H2D+2 syncs; should recover >952 and stack with the decode win. Then Stage D overlap.

**Stage C round 3 (same evening) - pp levers + attribution.** (a) Per-device EP-aware csort sort-cache (mcache_arr[GGML_CUDA_MAX_DEVICES], key incl. expert_base+n_local): BYTE-IDENTICAL, pp8192 884->**903.6 (+2.2%)**, ADOPTED (REUSE=1 now in EP deploy). (b) **PPL RUN-TO-RUN NONDETERMINISM CONFIRMED** on -sm tensor: two identical runs = 5.8340/5.8342 -> ALL 4th-decimal "drifts" today (5.8316/5.8341/5.8344) were this noise class (cuBLAS heuristic/alloc alignment); use +/-0.003 band for gates, delayed-AR change was numerically clean. (c) ub8192 CLOSED: meta compute buffer (ggml-backend-meta.cpp:1554 alloc, full-size per device) OOMs; fixed a REAL max_tmp_size accounting bug en route (tmp bufs sized by boundary node up to 4x too big vs the actually-reduced delayed node - kept, saves VRAM). (d) **nsys attribution of EP pp2048** (results/.../stageC-ep-nsys): NCCL allreduce bf16-ring-LL **31.1%** of kernel time (incl. rank-skew wait), GEMMs 31.2% (26.6k tiny maxwell_sgemm_fp16_128x32 per-expert calls), **get_rows f32 gather 11.3% + f32->f16 convert_unary 4.8% = 16% avoidable** -> (e) GGML_CUDA_MOE_F16GATHER implemented (gather src1 straight to f16, byte-identical by construction, kills the convert + halves gather writes) - gating/bench in flight. NEXT (ranked): NCCL_PROTO=Simple A/B at prefill sizes (decode-REJECTED verdict does not carry over), Stage D two-batch comm/compute overlap (the 31% NCCL slice), B2 CUTLASS grouped GEMM (the 26.6k tiny-GEMM wall).

**Stage C round 4 (same evening) - F16GATHER debugged: ROOT CAUSE was Q8_1_DEDUP, now FIXED.** F16GATHER initially produced deterministic garbage ("ilas") on long prompts. Bisect chain: identical garbage across two different GEMM implementations -> not the GEMM; per-stage stats (env `GGML_CUDA_MOE_DEBUG_STATS=<n>`, added to mul_mat_id: GATHER/GEMMOUT/SCATTER absmax/nan) all sane -> not numerics; minimal repro = 2 ubatches (1000 tok @ ub512), single ubatch clean; env bisect -> **`GGML_CUDA_Q8_1_DEDUP` stale cache hit**: its key (identity tensor ptr, src1->data, pool dst ptr, dims) is UNSOUND across graph evals - ggml arena tensor addresses, gallocr offsets and pool sequences all repeat with different content; f16gather's halved src1_sorted shifted pool layout into an exact collision. FIX: generation counter bumped in ggml_backend_cuda_graph_compute, stamped into the cache (mmvq.cu `ggml_cuda_mmvq_q8_1_cache_next_gen`); legit within-eval dedup hits preserved (decode byte-identical dedup-on vs dedup-off post-fix). NOTE: the SAME unsound dedup ships in the OTHER prod worktrees (build-p100-mmvq-batch, nccl-mtp-compose) - layout-lucky there, but if garbage ever appears, this is suspect #1. Also learned: F16GATHER is NOT byte-identical to OFF (small-m expert slices dispatch mmvq int8 in OFF vs f16 GemmEx in ON - in-family precision, gate via ppl +/-0.003 band + greedy coherence, both PASSED post-fix: ppl **5.8309**). Perf on fixed build (r4gates, 3-round interleaved): pp8192 ctrl 899.1 -> f16g **949.5 (+5.6%)**, pp2048 845.3->872.3 (+3.2%); NCCL_PROTO=Simple adds NOTHING at prefill -> dropped for good. ADOPTED into deploy-35b-moe-ep.sh (+ ep-soak env). Prefill ladder: 539 -> 659 (A+) -> 903.6 (EP+delayedAR+reuse) -> **949.5 (f16g)** with the +20% decode kept. Final gates on full stack: soak **PASS 20/20** (criterion patched: immediate-EOG with stop=true = valid completion, seed-dependent on the degenerate repeated prompt, A/B'd vs OFF = sampling-luck class), tg256 **75.47+/-1.53** (decode unaffected by f16g+dedup-fix, matches 75.3 historical).

**P1 submission threads (2026-07-21 late) - ADOPTED, +29.3% pp8192 (941->1217).** Persistent per-device worker pool in ggml-backend-meta.cpp ([TAG_META_SUBMIT], env `GGML_META_SUBMIT_THREADS=1`, in EP deploy): backends 1..3 on pinned workers + backend 0 inline, join before the (unchanged, main-thread) NCCL allreduce; engages ONLY on MUL_MAT_ID graphs >=64 tokens (decode threading = known Stage-1 reject). pp2048 870->1175 (+35%), tg neutral, server-path 975 pp + clean exit. Overlaps the per-device HOST work in the mul_mat_id fallback (csort D2H/sort/H2D + per-expert cuBLAS launches) that single-threaded issue serialized (ik #1080 -sas precedent). ACCURACY: ppl identical 5.8309; KLD save-all-logits pairs (c2048x4) = the decisive instrument: **OFF-self CONTROL is ALREADY nondeterministic (max KLD 5.9e-5, mean ~0) - pre-existing COMPUTE_32F cuBLAS wobble (no32f self: 7e-6), ships in prod today**; ON-self == control, ON-vs-OFF == control, same-top 100.000% over 8192 tok in every pair => threads add ZERO measurable noise; the 1-token greedy flip seen in gating = pre-existing borderline-token noise surfaced by timing. METHOD: single greedy completions are a WEAK nondet assay (2-run self-diffs ~coin-flip - first env bisect's "deterministic" verdicts were luck); per-call-sync debug stats SUPPRESS the flip (heisenbug); use KLD pairs. Results+scripts: results/qwen36-35b-moe-pp-20260721/p1-submit-threads/ + bench/{bench-p1-sas,p1-gates,p1-bisect,p1-kld}.sh. Ladder now 539 -> 659 -> 952 -> **1217**.

**P7 post-P1 nsys attribution (2026-07-22 morning) - GPUs only ~48% BUSY at pp2048; bottleneck = host-paced refill, not compute.** Trace p7-nsys-postP1 (sqlite queries, per-device): idle gaps 651-689ms before EVERY NCCL kernel (~6.4ms each) + 466-492ms before the allreduce f32<->bf16 converts + only ~100ms csort gaps (P1 did its job). Host side: the csort ids D2H (2MB pageable std::vector, ggml-cuda.cu ~2806) BLOCKS the API call ~20ms (full stream drain + cross-thread pageable-staging lock contention, ~500ms/thread blocked); mcache exact-fit realloc churn = 20ms cudaFree spikes; then main thread serially issues 4 devices' convert->NCCL->convert. Duty cycle [20ms drain: GPU busy] + [22ms refill: GPU EMPTY] = 48% PASS. NCCL "27%" is symptom (late enqueue + LL spin), wire ~15%. Full model: p7-nsys-postP1/ANALYSIS.md. **P8 GATED same morning (commits 4a8ddb3f5+64710dc7b, p8-spmd/RESULTS.md) - but the EP4 granularity fix partially overturned the P7 attribution first** (the huge pre-NCCL gaps were the pair-split straggler artifact; post-EP4 busy% already 72%). Verdicts on top of EP4: **P8a PINNED ADOPTED (+2.1% pp2048 = 1521, +0.8% pp8192 = 1470, byte-identical all legs, soak PASS 20/20, in deploy)**; **P8b SPMD FLAT (+/-0.2%) - engagement CONFIRMED via WARN log line, real verdict; keep default-off** (per-thread NCCL sandwich issue was NOT the binding constraint; code kept as Stage-D skeleton); P8c headroom unconditional (its 1-2% is inside the EP4 headline). METHOD TRAP: ggml GGML_LOG_INFO lines are FILTERED from llama-completion/server stderr - engagement checks must log at WARN (even "GGML_CUDA_GRAPHS_PRE_AMPERE set" never appears). Residual post-P8 idle = dependency wait around the ids D2H drain itself (host can't know per-expert counts before router executes) => ONLY remaining levers: Stage D two-batch / GPU-side grouped GEMM (B2-class) / AsyncEP. Ladder: 539 -> 659 -> 952 -> 1217 -> 1460-1488 (EP4, other session) -> **1470-1521 (P8a)**.

**CONCURRENT SESSION (2026-07-22 morning): EP 4-way granularity fix.** A second session (ep4-granularity results dir) found the EP expert split was effectively PAIR-split ([0,128,0,128] - 2 devices at ne12*4 pairs instead of 4 at ne12*2) and fixed it in src/llama-model.cpp (+9 lines, deliberately left UNCOMMITTED by me - it's theirs, under their gates vs baseline 1175/1217/72.1). Binary since 09:05 contains BOTH changesets; my P8 control leg therefore includes their fix. Coordination note: bench/COORDINATION-20260722.md. GPU serialization via idle-wait in p8-gates.sh.

**Stage D TBO (2026-07-22, session B) - BUILT, DEBUGGED, MEASURED: ZERO-SUM, NOT ADOPTED; overlap lever class CLOSED on P100.** Cross-eval CUDA-stream alternation in the meta/CUDA backends (env GGML_META_TBO + LLAMA_GRAPH_REUSE_DISABLE, default OFF): consecutive prefill evals on 2 streams/device, subgraph-index-aligned event fences for KV/GDN state, depth-2 bound via end-of-eval wait, per-slot NCCL comm sets. Gates: BYTE-IDENTICAL to prod (ctrl==noreuse==tbo, the scheduling-only goal held exactly); nsys shows both streams balanced and busy 80%->91% - THE OVERLAP WORKS - and wall is UNCHANGED (pp8192 1474.6->1478.1, pp16384 1359.8->1361.0): P100 SMs TIME-SLICE concurrent streams, so filling host-paced gaps with the other eval's throughput-bound GEMMs inflates kernel-seconds ~14% and returns zero. KERNEL overlap is zero-sum on this rig; only copy-engine overlap is free. Residual ~20% steady idle reachable ONLY via faster host dispatch or fewer kernel-seconds (B2/N6 grouped GEMM), NOT overlap. Also: reuse-disable alone HALVES decode (74.6->33.7 tg, rebuild ~8ms/tok) - never ship it globally. ATTRIBUTION DRIFT WARNING: the "GPUs 72% busy / 28% idle" figure was single-eval pp2048 pre-P8a; prod-shaped d4 prefill is already 80% busy. THREE REAL BUGS fixed en route (kept): (1) latent EP fallback n_local==0 zero-grid launch abort (degenerate tied router; unconditional fix); (2) ggml_cuda_op_mul_mat HARDCODED STREAM 0 (now honors curr_stream_no - poison for any multi-stream future); (3) NCCL comms are NOT stream-safe - collectives from 2 streams on one comm silently corrupt (states decay to zeros over layers); per-slot comm set fixes it. mcache/P8a-pin made per-(device,slot). Debug envs kept: GGML_META_TBO_SERIAL/_ONECOMM/_MIN_TOKENS; CUDA_LAUNCH_BLOCKING deadlocks with NCCL. Details: stageD-tbo/{RESULTS,DESIGN}.md, bench/tbo-gates.sh.

**P7 AsyncEP (2026-07-22, session B) - BUILT, MEASURED, REJECTED (-37%/-39% pp8192/pp4096; correctness clean).** Full weight-gather EP implemented (env GGML_CUDA_MOE_ASYNCEP default-OFF, kept like B1 - NEVER enable): per-device rotating prefetch buffer (6 slots x 285MB, ev_ready/ev_done fencing, peer-copy assembly of all 256 experts 2 layers ahead), token-slice csort mode (foreign tokens -> ZERO_ROW so the PARTIAL allreduce combine stays exact - mixed evals safe, no boundary change needed), shard registration meta->CUDA via proc-address, + NEW ncclAllGather comm primitive (dormant, correct). REJECTION IS STRUCTURAL: (1) GEMM THINNING - the expert filter IS the m-concentrator: expert-EP = 64 GEMMs/device @ m~128; token-slice+all-experts = 256 GEMMs @ m~32 = 4x launches (the known host wall) x 4x thinner maxwell_sgemm; ANY all-expert-residency scheme pays this. Round-2 memory's "fatter GEMMs/bit-identical 1.35-1.59x" transfers only to NVLink/comm-dominated regimes. (2) all-pull (3 peers each, simultaneous) gets well under the ring-measured 11.96GB/s (PCIe tree sharing) so the fixed 25.7GB/eval gather paces the pipeline. (3) every host-pipeline win shrinks the hiding window (post-EP4 NCCL wire ~13%). PCIe gate C DID pass (copies vs SMs: zero contention, bench/p7-pcie-concurrency.cu v2 - v1 had a pacing bug: never pace GEMM issue by a blocking ring pass). Comm-dataflow-inversion class CLOSED for this rig unless a B2/N6-class grouped GEMM solves thinning. NEXT (measured order): Stage D two-batch overlap (~28% drain idle), EPLB-lite (skew 1.24), B2/N6. Details: p7-asyncep/{RESULTS,DESIGN,IMPL-SPEC}.md.

**EP4 granularity fix (2026-07-22, session B) - GATED + ADOPTED: pp8192 1217->1460 (+20%), pp2048 1488 (+27%), tg128 73.2.** Root cause found via the round-2 "expert-load skew" gate on P1's moe-dbg artifacts: sum(n_local) over printing devices == ne12*8 with only ONE device pair per layer ({0,2} or {1,3}, period-8 ABAABABB from get_il_eff rotation) => "Stage C 64 experts/GPU" NEVER actually held. get_split_granularity applied the dense-FFN row granularity lcm(blck_size,128)=128 to the EXPERT axis (ne=256), collapsing split points to {0,128,256} => ne=[0,128,0,128] rotated: every MoE layer ran its experts on 2 of 4 GPUs, the other 2 idled then showed up as straggler wait INSIDE the NCCL kernels (inflating the historical "31%/27% NCCL" slices). Per-device VRAM stayed balanced (half the layers each), which hid it at load. FIX: src/llama-model.cpp get_split_granularity [TAG_MOE_EP] - under GGML_CUDA_MOE_EP, ffn_*_exps.weight granularity {1} (blck_size only constrains rows WITHIN an expert). Gates (bench/ep4-gates.sh): 4-way n_local spread exact in 120/120 groups; residual ROUTER skew max/mean 1.24 mean / 1.49 p95 (the true EPLB/N2 prize now); ppl 5.8340 in-band; greedy coherent; NOT byte-identical to the pair-split build (partial-sum partitioning changed). Headline includes session A's unconditional mcache headroom (~1-2%). Post-fix nsys (ep4-nsys, pp2048@nsys 1415.7 vs 1085.8 pre): GEMM 37.1%, NCCL 22.5% (abs 2.22->1.94s), gather/copy 16.8% (abs UP 0.93->1.44s - smaller per-device gathers less efficient, small-harvest candidate), GDN 7.9% (round-2 gate #1 ANSWERED: chunked-GDN N3/N4/N7 ceiling modest). Ladder: 539 -> 659 -> 952 -> 1217 (P1) -> **1460-1488 (EP4)**. Results: ep4-granularity/RESULTS.md.

**BUILD A grouped GEMM (2026-07-22, round-3 exec) - BUILT+INTEGRATED+GATED, env `GGML_CUDA_MOE_GROUPED=1`; e2e +1.1% pp8192 / +5.2% pp2048, ppl PASS (5.8312 vs 5.8332, delta -0.002 DOWN), and it PROVED the wall is HOST-SUBMIT-BOUND.** Custom kernel `ggml/src/ggml-cuda/moe-gemm.cu` replaces the per-expert dequant+GemmEx loop in the mul_mat_id fallback with ONE launch per matrix (240 vs ~48k launches/pass): 32x64 tile, 128 thr, 8x8 microtile, 4-way split-K, KSTAGE-32 double-buffer, interleaved XOR-swizzled smem ((k>>3)&3 term = per-thread-constant -> LDS.128 reads, conflict-free, zero inner-loop cost - the fix that padding could NOT reach), reads NATIVE Q8_0 34B blocks (no repack, no VRAM, MMVQ decode untouched - decode exits into MMVQ before the seam), dequant d*q in fp32 = EXACT (fp64-sampled ~300x closer to truth than prod's fp16-rounded dequant; ppl moved DOWN). Kernel ladder (bench/a1-gemm, m32.cu->v6): naive 2.23 -> v4 smem-union 2-CTA 5.84 -> v5 swizzle 5.97/6.27 TF/s @m=1024 = CUDA-C plateau (64-67% peak; v6 KSTAGE-16 4-CTA regressed 4.15 - sync freq beats occupancy). Microbench vs prod-semantics baseline on REAL hist-code routing: 1.55-1.89x (planes variant 1.73x needs in-place repack + MMVQ addressing surgery = A4c follow-up, +20% kernel). NSYS ON-vs-OFF (grouped-20260722/): expert slice 22.4%->16.2% of wall, kernel-time saved 739ms/GPU/pass (6.4% of wall) but wall -1.8% only and busy% FLAT 73.3->73.8 => **savings became idle; kernel-seconds cuts pay ~25% of face value until the host drain is broken. NCCL bf16 now the #1 kernel (20%), get_rows gather/scatter 12.7% (A3 fusion target = launches AND kernel-time), dense 32x128 9%.** ub sweep: ub4096 STAYS optimal (ub8192 asserts meta:1558 both arms = pre-existing OOM, not grouped). Next levers ranked: host op-count reduction (A3 fused gather, MoE-block fusion, graph-capturable prefill via device-side tile table), NCCL overlap/compress (P5 still policy-deferred), only then A4c/SASS. Results: round3-phase0/RESULTS.md + grouped-20260722/.

**A6 DEVICE PLAN (2026-07-22 evening, reviewer-endorsed) - ADOPTED, +11.0% pp8192 (1490.8->1654.5), +9.5% pp2048 (1753.5); the host-drain diagnosis CONFIRMED BY INTERVENTION.** Sync-free mul_mat_id path (env `GGML_CUDA_MOE_PLAN=1`, moe-gemm.cu [TAG_MOE_PLAN]): 2-kernel device planner (single-CTA histogram/scan/M32-tile-build + fixed-grid atomic scatter; UNSTABLE within-expert order is safe - each output row depends only on (token,expert), from_sorted is the exact inverse) + persistent fixed-grid (2*nsm CTAs) grouped Q8_0 GEMM grid-striding over device-resident n_tiles with IN-KERNEL f32 gather (A3 - reads ORIGINAL f32 src1, no f16 gather pass, activations unrounded) + existing capacity-launched inverse scatter with ZERO_ROW moved to the shape-static CAPACITY slot. NO ids D2H, NO stream sync, every launch shape-static => A7 (existing whole-graph CUDA capture, relax [TAG_MUL_MAT_ID_CUDA_GRAPHS]) is the follow-on. Workspace = persistent per-(device,slot) cudaMalloc, NOT pool (addresses must survive graph replay - Q8_1_DEDUP lesson). GOTCHA FOUND: first cut gated ne11==1 which EXCLUDED the down projection (its src1 = GLU output with ne11=n_expert_used) -> one full host sort+drain per layer remained AND still gave +6.3-6.8%; general-ne11 fix (to_sorted = i12*ne11+iex%ne11, contiguous dim-1/2 eligibility) completed engagement. Gates: PLAN_CHECK 64/64 vs host reference (gate/up/down, both EP shards), ppl 5.8318 in-band, soak PASS 20/20, decode untouched. Perf accounting: the sync-bubble removal bought ~7x what Build A's kernel-time cut alone bought (kernel-seconds were never the binding constraint at ub4096). Fallbacks to host path: MOE_HIST/MOE_DEBUG_STATS envs, asyncep, non-Q8_0. Ladder: 539 -> 659 -> 952 -> 1217 -> 1460-1488 -> 1470-1521 (P8a) -> ~1494 (grouped) -> **1654 (A6 plan)**. Results: grouped-20260722/ + round3-phase0/RESULTS.md.

**A7 graph capture (same evening) - BUILT+VERIFIED, FLAT, env-gated OFF ([TAG_MOE_PLAN_GRAPHS], `GGML_CUDA_MOE_PLAN_GRAPHS`).** MUL_MAT_ID graph-compat blocker relaxed when the plan path will handle the node (predicate must mirror runtime engagement EXACTLY or a sync fires mid-capture); workspace growth guarded by cudaStreamIsCapturing assert. Capture works perfectly on P100: 648 chunk-graphs/eval (162/device - the meta backend already chunks at NCCL boundaries, collectives naturally outside), converges pass 2, pure replay after, zero churn, syncs 856->360; one-time cudaGraphInstantiate 8.86s (~13.7ms/graph pre-Ampere node-count scaling, amortizes in server use). A/B r12x2: 1654.6/1654.4 vs 1653.5/1654.9 = **FLAT** - A6 removed the dependency STALL and P1 threads had spare submit throughput, so replay saves host cycles nothing needed (TBO-class zero-sum, submit axis). Post-A6 idle ~25% = NCCL arrival skew + true dependencies. KEEP OFF; mechanism banked for future host-cost growth. Next levers reranked: NCCL slice 20% (P5 Q8-wire POLICY-DEFERRED; EPLB/B2 code repack ~3-5% for the 1.24 arrival skew), A4c planes / SASS (~2-3% each, kernel-seconds pay ~face value at 75% busy), H2 K=1 MTP.

**Items 1a+1b (2026-07-22 night, post-A7) - BOTH ADOPTED, BOTH BYTE-IDENTICAL.** (1a) p-fold: try_fuse {MUL_MAT_ID, MUL} folds the routing-weight multiply into the down-GEMM plan epilogue (to_sorted IS the assignment index for the down matrix = flat weights index; multiply moves before the inverse scatter). Greedy sha256-identical fold vs nofold; pp8192 1641->1686 (+2.8%), pp2048 +2.9% (also deletes the full [2048,8,N] read+write pass); soak PASS; kill-switch GGML_CUDA_MOE_PLAN_NOFOLD. (1b) EPLB: load-time expert permutation in meta set_tensor ([TAG_MOE_EPLB], env GGML_CUDA_MOE_EPLB_MAP=bench/eplb-map-code.txt): permutes ffn_*_exps expert axis + ffn_gate_inp rows into host scratch BEFORE split dispatch -> EP4 slices = calibrated bins; ids live in permuted space, runtime untouched; no exp_probs_b in this GGUF. Greedy BYTE-IDENTICAL. **METHOD TRAP: llama-bench synthetic prompts are the WRONG test for a code-calibrated map (domain-disjoint hot sets) - measured FLAT/-0.3% there but +1.4% consistent on REAL code prefill (llama-completion 14.6k-tok source, 1563->1585)**. Adopted (rig serves code). Skew->wall sensitivity is SUB-LINEAR post-A6 (in-sample skew 1.25->1.00 bought only 1.4%): remaining idle is mostly NOT arrival skew -> demotes further EPLB work; DMA copy-engine allreduce (item 2) is the open lever. Code-prefill ladder basis: ~1585; synthetic pp8192 ~1686.

**Items 2+3 (2026-07-23) - AR_P2P allreduce BUILT+FIXED+GATED (+0.8-0.9%); fp16 HFMA2 axis KILLED by measurement.** (2) [TAG_AR_P2P] allreduce.cu peer-DMA permutation-round allreduce (env `GGML_CUDA_AR_P2P=1`, >=4MB f32 boundaries only, decode stays NCCL): bf16 wire, owners sum 4 shards in f32 (ONE rounding), allgather DMAs into peers' dead wire slots, host-serial event chain (no flags/atomics). Duplex probe: 12.51-12.52 GB/s per link full-duplex/ring-round (95%+ of uni) vs NCCL 9.9 on SMs. **BUG CLASS FOR THE RECORD: custom collectives must replicate the NCCL wrapper's INACTIVE-SHARD ZEROING (GGML_TENSOR_FLAG_COMPUTE) - summing non-computed devices' uninitialized tensors gave degenerate logits (negative log-prob stddev) that greedy smoke CANNOT catch (only ppl full-position eval did)**. Post-fix: ppl 5.8335 in band, pp2048 +0.9% (1805), pp8192 +0.8% (1697). v2 upside open: chunked pipelining (v1 stage barriers amplify arrival skew vs NCCL's pipelined ring). (3) m32h2.cu microbench = the reviewer's exact S2-tree/block-32 geometry (half2 lane k-striping, per-32-block fp32 flush): **0.89x/1.00x vs the fp32 mainloop (5.89/6.27 vs 6.64/6.28 TF/s) - kill threshold 1.55x FAILED decisively**; block-32 flush overhead + narrow half2 smem reads consume the entire HFMA2 issue advantage, and Q8 exact scales FORCE block-32. fp16-GEMM axis CLOSED for prefill too (joins the 7x-refuted decode matvec class); dismiss future block-FMA/HFMA2 GEMM proposals against results/qwen36-35b-moe-pp-20260721/round3-phase0/RESULTS.md items 2-3. CLOSE-OUT: AR_P2P ADOPTED after a SECOND gate-caught bug - the EPLB full-tensor host scratch OOM-killed the --no-mmap server load (model fills the 45GB host; mmap-based tests never saw it; journalctl confirmed oom-kill). Fixed scratch-free (per-expert gather inside the AXIS_2 set_tensor branch, byte-identical). Full-stack soak PASS 20/20. **Adopted prod stack now: CSORT+EP+REUSE+F16GATHER+THREADS+PINNED+GROUPED+PLAN(+p-fold)+EPLB_MAP+AR_P2P; ladder synthetic pp8192 ~1697 / pp2048 ~1805 / code-prefill ~1585-1600; decode 74.6.** RIG LESSON: any load-time transform needing host staging must be sized against --no-mmap's full-model host residency.

**PascalWave cyclic virtual-pipeline prefill (2026-07-22 late) - BUILT (P0/P1), MEASURED, REJECTED at the kill gate; STRUCTURAL blocker identified.** External proposal (user-approved plan + kill gate pp8192>=1850): cyclic layer blocks + token-chunk wavefront at -sm layer to replace EP4's collectives/skew. P0 control (stock CONTIGUOUS layer split, modern kernels incl GROUPED+PLAN, GGML_CUDA_P2P=1, no EP/NCCL): pp8192 ub4096/2048/1024/512 = 1173/1410/**1468**/1368, tg 61.9 - the old 659 layer baseline was kernels+fill, not the class ceiling; contiguous PP already pipelines via stock sched (n_copies=4, async submit; graph reuse NEVER fires between prefill ubatches - growing n_kv breaks can_reuse - so the reuse-sync at llama-context.cpp:1334 is moot at prefill). P1 CYCLIC ([TAG_PW_CYCLIC] env LLAMA_SPLIT_CYCLIC in llama-model.cpp get_layer_buft_list, kept dormant): blk1/2/4 = 696/701/770 = FULLY SERIALIZED, ub-trend INVERTS (more chunks worse). Input-copy-dedup theory tested and REFUTED (no-op, patch reverted). **ROOT CAUSE: ONE CUDA STREAM PER BACKEND - under cyclic placement ubatch k's LAST virtual stage queues on each GPU's in-order stream BEFORE ubatch k+1's FIRST, so cross-ubatch overlap = zero. Contiguous survives because each GPU appears once, early. A real wavefront needs per-virtual-stage streams + event-only inter-split deps = TBO-class ggml_backend_sched rebuild (ceiling est ~2200, undemonstrated). The proposal's "llama.cpp is surprisingly close" premise is measurement-refuted.** Verdict vs gate: best cyclic 770 vs 1850 (2.4x miss); best layer-split of ANY kind (contiguous ub1024, 1468) is 11% below EP4 prod 1654 WITH -17% decode - no deployment case. [TAG_PW_NOREUSE] also dormant. Results: pascalwave-20260722/RESULTS.md; bench/pw-p0/p1/p1b.sh. Road A (p-fold epilogue, EPLB code repack, DMA copy-engine allreduce) resumes as plan of record.

**Stage C round 1 (earlier same evening) - first COMPLETE results.** All 4 edits done incl. (4b) decode: mmvq.cu EP-aware (`ep_base_enc`+`nchannels_x` threaded into `mul_mat_vec_q` [ncols=1 early zero-write return] and `mul_mat_vec_q_moe` [skip-accumulate->zeros]; stamp read in `ggml_cuda_mul_mat_vec_q` handles fused-GLU dst via `dst->src[0]`; MMQ/MMF mmid gated off under EP). **MEASURED (interleaved 3-round, r1 dropped, prod env stack):** pp2048 892.6 vs layer 652.5 (+36.8%); pp8192 **952.4 vs 659.3 (+44.5%)**; tg128 64.3 vs 62.5; tg256 **67.7 vs 62.7 (+7.9%)**. Ladder: 539 -> 659 (A+) -> **952 (EP)**. Perplexity prod-matched (-c4096 -ub4096, 8 chunks): EP **5.8316** vs layer 5.8319 - exact to 4 sig figs. Greedy decode coherent/reference-grade (single-token fp-noise divergence vs layer, expected across split modes). VRAM 10.2-10.6GB/GPU @32K f16 KV (vs layer 11.6); session-1 "device-0 OOM at ub4096" NOT reproducible after final edits. **KNOWN ISSUE (exit-only):** with GGML_CUDA_GRAPHS_PRE_AMPERE+SPLIT_BUFFER + EP decode, process aborts at teardown in ~ggml_backend_cuda_context (cudaStreamDestroy dev3, "unspecified launch failure") AFTER generation+stats complete; without graph flags exit is clean but decode drops 58->34 t/s -> keep flags, fix later (suspect in-flight-graph vs free-order race; upstream #24937 region, do NOT rebase). Prefill/ppl exit clean. **NEXT:** server soak+toolcall gate before prod switch; MTP/spec compose under EP untested (35B spec=none anyway); Stage D overlap on top of EP. Historical session-1 notes:  Env `-sm tensor -dev CUDA0..3 GGML_CUDA_MOE_EP=1 GGML_CUDA_MOE_CSORT=1`. Loads past the old L1568 3D-expert assert; the full PARTIAL inference cascade (gate/up->PARTIAL, glu, down, MUL-by-weights, allreduce combine) works. KLD-clean: EP ppl 6.1722/6.1624 vs layer-split 6.1674/6.1649 (matched -c2048 --chunks4, within error bars). FASTER (perplexity s/pass, matched): ub512 EP 420 vs LS 361 t/s (+16%); ub2048 EP 569 vs LS 455 t/s (+25%) - advantage GROWS with ub. Details `results/.../stageC-EP/EP-VALIDATED.md` + `ep-full.diff`. The 4 edits that made it work: (1) llama-model.cpp expert AXIS_2, (2) handle_mul_mat AXIS_2 MUL_MAT_ID (src1 MIRRORED|PARTIAL) -> `assume_sync?MIRRORED:PARTIAL`, (3) init_tensor_impl op_params[15]=expert_base+1, + handle_bin_bcast MUL(PARTIAL,MIRRORED)->PARTIAL, (4a) ggml-cuda.cu counting-sort fallback EP-aware (map global->local, skip non-local, ZERO_ROW zero-fill). **REMAINING:** (i) MEMORY - device 0 OOMs its 8.8GB expert shard (35GB/4) at ub4096/large-ctx -> capped at ub<=2048@-c2048; must reduce device-0 MIRRORED pressure (embed/output/shexp) or use tensor_split weighting to unlock ub4096 (where EP should beat the 660 prod ceiling). (ii) DECODE edit 4b - mm_ids_helper (mmid.cu) NOT yet EP-aware -> tg wrong; apply same expert_base compare. Full design/status: `results/.../stageC-EP/{DESIGN.md,STATUS.md}`. Approach "Option 1" (per-device op_params expert-base offset). Gated by `GGML_CUDA_MOE_EP=1`+`-sm tensor`; `-sm layer` prod untouched (verified pp8192 666/tg 62.6 with edits in). DONE + compiles clean: (1) assignment `ffn_(gate|up|down)_exps.weight->SPLIT_AXIS_2` (llama-model.cpp, 64 experts/GPU), (2) inference `handle_mul_mat`: MUL_MAT_ID+AXIS_2+MIRRORED->PARTIAL (ggml-backend-meta.cpp ~575), (3) offset stamp op_params[15]=expert_base+1 in init_tensor_impl (~1176). KEY FACTS: only cross-device reconciliation is PARTIAL->NCCL-allreduce (no all-gather); mm_ids_helper (mmid.cu) hard-assumes local-block==global-id, op_params copied verbatim -> EP needs the new offset channel; 256/4=64 exact (no zero-slice). REMAINING = edit 4 (CUDA kernels): (4a) prefill counting-sort fallback - map global->local, skip non-local, ZERO_ROW zero-fill (currently OOBs tokens_per_expert[256] under EP), testable via perplexity; (4b) mmid.cu decode compare `expert_used==expert_base+expert`. Full spec in STATUS.md. **NOT STARTED:** Stage D (overlap) * Stage E harvests * B2/CUTLASS.
